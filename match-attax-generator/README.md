# Match Attax Generator

Project documentation now lives at the repository root (`../README.md`). The source for the Next.js app remains in this folder.

Quick start reminder:

```bash
npm install
npm run dev
```

For contribution guidelines, tooling expectations, and licensing notes, see the root `README.md` and `../agents.md`.
